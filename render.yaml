services:
  - type: web
    name: vee-music
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false
